container_name: backend
stateless: true
default_service_runner: docker
supported_service_runners:
  - local
  - docker
service_runners:
  local:
    envfile: .env
    build: npm run install && npm run dev
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - 3000:3000

container_name: pgadmin
stateless: true
default_service_runner: vmdocker
supported_service_runners:
  - docker
  - vmdocker
service_runners:
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - 8080:8080
